# 首页改版工作日志（redesign-home 分支）

> 本文档是跨会话/跨设备的交接说明。任何新会话接手前先读完本文档。
> 改版原则：**一小块一小块做，每块做完用户过目确认 → commit + push → 再进下一块。**
> commit/push 一律由用户自己执行，AI 不主动提交，只提供提交信息（2026-09-15 用户定）。

## 一、项目背景

- 改版对象：`yl-doc`（VitePress 1.6 + vitepress-theme-teek 1.5）的首页
- 分支：`redesign-home`（从 master 拉出），master 上是旧版首页
- 产品本体：`yl-editor`（工作区兄弟目录，Path of Exile 过滤器在线编辑器，edit.filtereditor.cn）
- 首页内容基于 yl-editor 真实功能（POE1/POE2 编辑页为核心），**不受编辑器 UI 风格影响**（编辑器只是 Naive UI 组件库堆的，无特殊风格）
- 用户设备偏好：桌面用户为主（编辑器要求桌面 Chrome/Edge），移动端策略 = 排版适配 + 动画从简（不做交互级动画适配）

## 二、设计定案（已拍板，不要重新讨论）

- **风格 A「暗金 ARPG × 编辑器语法」**：近黑底 + 金橙主色（品牌色 #E8A23C）+ POE 稀有度色板点缀；等宽过滤代码作装饰纹理；功能卡做成"游戏物品名牌"形态。排版克制、大留水。设计语言取自游戏与编辑器本身（物品名牌、T 档配色、光束、地图钉、Show/Hide 语法）
- **首页固定深色**：进入首页强制 `html.dark` + 隐藏导航栏明暗切换按钮（`html.yl-home-active .VPNavBarAppearance { display: none }`），离开恢复用户原主题。不做亮色版首页
- **移动端**：不做"套旧版"，新首页响应式；排版适配 + 动画从简（不做交互级动画适配）
- **动画与系统动效偏好（2026-09-15 定）**：**不读 `prefers-reduced-motion`，任何系统设置下动画一律执行**（起因：用户公司 Win11 关闭「辅助功能-动画效果」，浏览器上报 reduce，页面动画全部消失）。**后续所有区块禁止新增该判定**，验收不再有"减少动效"态
- **动画库**：gsap 3.15（已装，3.13+ 插件全免费），SSG 环境所有 GSAP 代码只在 `onMounted` 跑

### 页面结构蓝图（7 段）

```
01 Hero          主视觉：入场动画 + 双CTA(打开编辑器/下载懒人包) + 数据带(163,294位流放者 + POE1 v3.29.2 / POE2 v2.0.8 双版本徽章)
02 签名叙事区     满屏混乱掉落 → 过滤生效 → T档分层；先做 before/after 滑杆版，后续可升级滚动叙事(scrub)
03 功能长廊       区块大标题「一乐过滤能做什么？」+ 五幕依次向下排的普通区块：可视化编辑(基础功能打头) → 物价自动排序 → 官网云同步 → 自动更新(五类差异合并) → POE2 规划器(10类通货tab轮播)。幕序 2026-09-15 用户定：按"基础 → 高级"讲，功能清单以用户指定为准，每幕独立小验收。结构定案（同日）：幕间靠编号眉题/间距/分隔线，交替的是左右布局（第二幕起镜像）不是滚动机制；每幕按内容自选形态（交互/自动循环/入场浮现/需要时单独上 sticky），滚动叙事全页仅签名区一处
04 Bento 速览    主题市场/色板库/本地直读直写/纯净模式/音效包(妹妹音效可hover试听)，不对称bento + hover微倾斜发光
05 社区生态带     QQ群/B站12集教程/定制服务/biubiu合作位 → 横向生态带（不再占主视觉）
06 CTA 终章      大字 + 光束汇聚 + 双按钮 + 版本公告
```

## 三、当前状态

**✅ 已完成：地基（第 1 块）**，验证全通过（桌面/构建/移动390px无溢出/明暗三态切换）

**✅ 已完成：02 Hero（第 2 块，用户已验收定稿）**

最终结构（`HeroSection.vue`，全部细节以代码为准）：

- **内容纵列**：logo（/logo.webp，圆角+金色投影，入场第一个：缩放淡入）→ 大标题「一乐过滤」（SplitText 拆字逐字上浮）→ 金色渐变下划线（scaleX 从中间展开，4px 带辉光）→ 两行副标题（`国服最好用的过滤编辑工具` 大亮 / `简单、高效、灵活、高度自定义` 小灰）→ 三按钮（金色实底「打开编辑器」→edit.filtereditor.cn；描边「下载懒人包」→/download.html；描边「先看教程」→/base/intro.html）→ 数据带两行（`已有 N 位流放者加入` 17px/数字22px 金色 mono；版本行 `POE1 v3.29.2` `POE2 v2.0.8` = 金色小圆点+16px粗金色mono纯文本，非按钮样式）→ 底部 SCROLL 呼吸线
- **用户数**：SSR 初始为 0（避免"先显示最大值再归零"闪烁），入场末尾 fetch：`/api/v1/user/count` → 失败回退 `https://api.filtereditor.cn/prod/v1/user/count` → 再失败显示 "160,000+"；成功则 1.6s count-up
- **背景三件套**：① 锚定光晕（720×440 居中在 logo+标题后方，光有落点不显脏）；② 金色浮尘（JS 注入，桌面16颗/移动8颗，上浮淡出循环）；③ **7 个真实过滤掉落物名牌**（左列4：卡兰德的魔镜/毁灭者之弓/重革腰带/崇高石；右列3：神圣石/贪婪战书/高级混沌石），配色/字号取自用户真实过滤规则（游戏字号×0.32），数据驱动（`leftDrops/rightDrops` 数组：name/text/bg/border/fs/beam/dx/mt/bh）
- **掉落演出**：入场时依次从上方70px加速砸落（间隔0.22s）→ 落地名牌挤压回弹（elastic）→ 光柱从底部迸发；全部落定后 `tl.eventCallback("onComplete")` 启动待机（左右组漂浮 + 光柱错峰呼吸），避免与入场争夺 opacity
- **光柱三层结构**：炽亮核心（混白渐变+双辉光）/ 宽柔光锥（clip-path 收窄 + blur + CSS 呼吸）/ 底部椭圆光斑；颜色衍生 `mixWhite()` 计算 --beam-core
- **散落感**：每颗名牌独立 dx（水平偏移）/mt（垂直错落）/bh（光柱高度），打破垂直堆叠
- 名牌布局 <960px 隐藏；动画不读 `prefers-reduced-motion`（2026-09-15 起无条件执行）

| 文件 | 作用 |
|---|---|
| `docs/.vitepress/theme/components/home/styles/tokens.scss` | 全部设计 token：底色/金橙/稀有度色板/语法高亮/字体/间距/圆角/动效变量 + `yl-container`、`yl-item-label` mixin。**改风格只动这里** |
| `docs/.vitepress/theme/components/home/HomePage.vue` | 容器：挂 token、全宽覆盖(.VPContent:has(.yl-home))、强制深色+隐藏明暗按钮、顶部金色氛围光晕、区块公共空壳样式 |
| `docs/.vitepress/theme/components/home/sections/*.vue` | 6 个区块空壳（HeroSection/Signature/Features/Bento/Community/Cta），带锚点 id，逐块替换内容 |
| `docs/index.md` | `layout: page` + `<HomePage />`；旧首页在 git 历史（master） |
| `docs/.vitepress/theme/index.ts` | enhanceApp 全局注册 HomePage |
| `docs/.vitepress/teekConfig.ts` | 用户改了 `notice.initOpen: false`（公告不再自动弹） |

**✅ 已完成：03 签名叙事区（第 3 块，用户已验收完结；文案 2026-09-15 定稿）**

`SignatureSection.vue`，双模式同组件共存，共用一套真实物品数据：

- **真实场景数据（用户提供）**：21 个物品（粗制弓/毁灭者之弓/锈蚀胸甲/邪恶束衣/重革腰带/金光戒指/先祖冠冕/卡兰德的魔镜/神圣石/混沌石/崇高石/高级工匠石/富豪石/蜕变石碎片/引路石16阶/未切割技能宝石等级20/红玉/贪婪战书/梦魇拟像裂片/先祖密藏日志/万用腰带），颜色/字号/光柱/小地图图标全部取自真实过滤规则（字号 ×0.32）；无过滤态默认样式 `SetTextColor 150 141 121 / SetBackgroundColor 13 12 10`；粗制弓/锈蚀胸甲/蜕变石碎片 3 个过滤后隐藏；5 道光柱；13 个小地图图标热链 `https://edit.filtereditor.cn/images/drop/icon_{Shape}{Color}.png`（三档 18/14/11px）
- **滚动叙事模式**（桌面 ≥960px）：区块高 320vh，CSS `position: sticky`（不用 ST pin，更稳）钉住舞台；ScrollTrigger 只算进度 → `sweep` 0~108 驱动金色扫描线（带 Show 标记）从左往右；扫到的灰字名牌逐个坠落淡出（左右交错倾角），值钱名牌原地点亮（缩放浮现 + 光柱 scaleY 升起），三段文案随进度切换，完全可逆
- **滑杆模式**（移动端）：双层反向 clip-path（`inset(0 0 0 pos)` / `inset(0 calc(100%-pos) 0 0)`），向右拖=过滤从左往右生效，钳制 1%~99%
- 物品纵向分布 14%~78%（用户要求的紧凑度），18 个过滤后名牌零重叠
- **文案已定稿**（2026-09-15 用户提供）：标题「一眼看懂过滤是什么」；滚动描述「继续滚动，让过滤器持续加载」；滑杆描述「拖动分割线，看看过滤器是什么效果 —— 垃圾消失，各分类价值清晰可见」；phases = 满屏掉落，分不清价值？/ 加载过滤器 —— 垃圾消失 / 各分类价值清晰可见

**✅ 已完成：04 功能长廊 · 第一幕「可视化编辑」（2026-09-15 用户暂定稿）**

`FeaturesSection.vue`，纯交互演示（组件内**无** GSAP/ScrollTrigger/sticky/340vh）：

- 结构：区块大标题「一乐过滤能做什么？」+ 左文案右演示两列 grid；入场动画 = 左列卖点与面板行从下往上依次浮现（IntersectionObserver + 3s 兜底；js-anim 类由 JS 添加，无 JS 时内容保持可见）
- 迷你编辑面板 6 行控件：文字/背景/边框颜色色板、字号步进（钳制 12~45）、光柱开关、小地图图标开关；面板选中态只反映用户操作，不随任何滚动变
- 演示物品：神圣石，**默认即真实过滤终态**（红字 #ff0000 / 白底 / 红框 + CircleRed 图标 + PlayEffect Red 光柱）；预览完全由用户点击驱动，点色板/步进/开关实时变
- 素材全部在线直链（back1.png 地面 / cross-Red.svg 光柱 / icon_CircleRed.png），未复制进仓库（用户定的策略，与签名区图标一致）
- 名牌字号换算照抄编辑器 filter-block 公式 `(SetFontSize+15)/2` 再 ×1.5 便于观看；名牌+光柱组在地面内整体居中（垂直居中对齐，同编辑器预览排布）；光柱 0.5s 从底部升起/熄灭，图标弹入
- 文案：描述句与卖点三「光柱、小地图图标，一键开启关闭」、卖点四「全量规则可自定义」为用户定稿；标题「可视化编辑，所见即所得」与卖点一/二仍是占位，用户后续可改
- 移动端：单列竖排、控件可点、地面 200px、无横向溢出

**✅ 已完成：04 功能长廊 · 第二幕「物价排序」（2026-09-15 用户验收完结）**

`features/PriceSortAct.vue`，布局与第一幕镜像（演示左、文案右），纯交互无滚动联动：

- 文案：标题「物价排序，实时物价分级」+ 标题右上角 **POE1 徽标**（POE1 编辑页专属功能标记，后续 POE1 专属的幕同款）；描述句「……——物价变了，分类T级跟着一键同步」均为用户 2026-09-15 定稿；**卖点四条仍为占位待定稿**
- 演示面板同编辑器真实布局：**左侧物价列表参考**（原序固定，仅数值随数据源变）+ **右侧排序分级区**（未排序时虚线空态；点「按物价排序」后按价格降序分组浮现：第1名 T1 / 2~3名 T2 / 其余 T3，逐组错峰浮现，T1 金亮 T3 转灰）
- 五分类 tab：命运卡 / 暗金 / 圣甲虫 / 卓越宝石 / 星团珠宝（每类 6 物品）；国服/国际服双数据源切换，已排序时切源立即重新分档
- 物品名取自 yl-editor poe1 json 真实数据；**价格为示例数据**（用户校正过一轮），面板标「示例数据」小字，不请求线上接口
- 首次进视口自动演示一遍排序；入场从下往上浮现；移动端文案在上、主体竖排、无溢出
- **分档口径**：演示为简化三档（名次式）；真实编辑器为 T1~T5 五档价格阈值式（截图实测：T1≥1768C/T2≥353C/T3≥88C/T4≥10C/T5≥2C，随 DC 比浮动）——若要改成阈值式五档待用户发话
- 幕底配**编辑器实际截图**（见下方截图机制）

**📸 每幕截图机制（2026-09-15 起）**：`features/ShotThumb.vue` 公共组件——窗框缩略图（150px 裁切 + 降饱和暗化 + hover 恢复）+ 点击 lightbox（点背景/Esc 关闭、lazy）。截图由用户提供放 `docs/public/assets/index/`（现有：可视化编辑.webp / 物价排序.webp，均为深色模式、无敏感信息），接入只需传 src/caption/pos。每幕"品牌演示 + 实际界面"成为功能长廊固定节奏。

**✅ 已完成：04 功能长廊 · 第三幕「通货、宝石实时规划」（2026-09-15 用户验收完结）**

`features/PlannerAct.vue`，布局回正（文案左、演示右），纯交互：

- 顶层规划器切换（通货规划器 / 宝石规划器）+ 通货子分类 tab（符文 / 精华 / 预兆 / 灵核 / 雕像）；宝石规划器 = 血脉辅助宝石单分类
- 演示形态同第二幕：左参考（原序固定）+ 右分级（第1名 T1 / 2~3名 T2 / 其余 T3），点「按物价排序」生成、自动首播；**无数据源切换、无阈值行**（用户定：与 POE1 物价排序类似，不单独体现）
- 物品名全部真实（`json-data/poe2/通货.json` 五分类 + `血脉辅助宝石.json`），价格为示例值（E 单位）**待用户校正**；面板标「示例数据」
- 标题「通货、宝石实时规划」+ **POE2 徽标**；描述句「——档位跟着当前物价走」、卖点三「按实时物价自动分档，配色自由修改」已定稿，描述前半与其余卖点仍占位
- 双截图位：`通货规划器.webp` / `宝石规划器.webp`（待用户提供）；ShotThumb 已加缺图降级（图片不在则整块不渲染）
- 灯箱放大图升级：上限 1920px / 90vh
- **版本徽标定位定案**（POE1/POE2 两幕统一）：标题盒 `fit-content` + 徽标绝对定位右外侧（`right: -58px; top: -4px`，落在两列间隙，不占标题行宽、不压字）；移动端回退行内跟随。教训：行内放徽标会把长标题挤断行
- **待办**：两张截图、价格校正、文案余下部分定稿；**幕数待重排**——装备规划器单开第四幕后总幕数变化，云同步/自动更新是否保留待用户定，各幕眉题「/ 05」届时统一改

**✅ 已完成：04 功能长廊 · 第四幕「装备规划器」（2026-09-15 用户验收完结；标题与卖点一已定稿，余下文案待定）**

`features/EquipPlanAct.vue` + `features/equip-data.ts`，布局破例（特殊幕区域做大）：**文案横排在上**（左标题块 + 右卖点 2×2），**演示面板全宽在下**。顶层三 tab 同编辑器，面板标题与底注随 tab 动态：

- **黄蓝装规划**（复刻编辑器三视图，共享分级状态真联动）：
  - 已分级：T1/T2/T3 三列（红 #FF2800 / 橙 #E6A23C / 蓝 #409EFF），chip = 图标+中文名+×移出，空态「去快速分级添加」
  - 快速分级·武器：笔刷交互（选档位→点卡片分档/再点取消/点他档改档），弓全量 25 卡（图标+中文名+英文 BaseType+Lv+物伤/攻速/暴击），卡片区内部滚动（max 480px）
  - 快速分级·防具：4 防御列展示（纯护甲/纯闪避/纯护盾/护甲&闪避 = 3 纯+1 混合代表；数据文件保留全量 7 组分组，换列只改 `SHOWN_GROUPS` 一处），列内按属性和降序、行整行着档位色，列体滚动（max 430px）
  - 数据：`equip-data.ts` 由 `equipment.expand.data.json` 脚本生成全量真实基底（弓 25 / 胸甲 136；1 条无防御属性胸甲不入列，同编辑器行为）；预置分级口径（用户定）：T1 = 毁灭者之弓/寻战之弓/双子之弓 + 三纯组榜首（士兵胸甲/滑击背心/邪恶束衣），T2/T3 = 防具各展示组降序第 2/3 名
- **白装规划**（门槛填充 mini 版）：头部「国服当前DE比 148·示例」+ 门槛按钮 ≥5/20/60E + 追加/重置；左侧物价列表 12 条（75+ 顶段真实基底跨 12 类目 + 真实图标 + 孔数标记，价格示例值）命中金高亮、已收录绿点+绿左缘；右侧块 82白 / 3孔T1 / 3孔T2（点选目标、chip 实时写入、空块占位）；多孔块自动按孔数过滤；同一基底可收录于多个块（同编辑器）
- **暗金装规划**（门槛分档 mini 版）：头部「国际服当前DE比 152·示例」+ 两组门槛组合（T1≥50/T2≥15/T3≥5、T1≥100/T2≥30/T3≥10）+ 按物价排序/重置；左侧暗金列表 12 条真实暗金名（传奇装备.json text 拆分 + 基底图标，含经典「猎首 重革腰带 300E」）；右侧 **T0 单一基底只读区**（金框：库勒马克之握/脆弱的诗文/腐朽迸发；单一基底=基底仅一个暗金、价值可精确确定，447 个暗金中 324 个）+ T1/T2/T3 门槛组（档标+带价 chip 错峰浮现）+「其余进剩余全部传奇块」；已排序时切组合实时重算；暗金视图左列收窄（5fr/7fr）
- 幕底一行五张编辑器实际截图（`装备规划器1~5.webp`，用户提供，按 tab 顺序排：1 黄蓝已分级 / 2 黄蓝快速分级 / 4 白装 / 3 暗金 / 5 暗金分档编辑；ShotThumb 缺图降级，移动端单列）
- 入场 data-rise 错峰（IO+双rAF+3s 视口内兜底）；**无自动演示**（用户定纯手动）；图标全走 edit.filtereditor.cn 直链（@error 自动藏图）
- 文案：标题「装备规划，T级精准操控」+ POE2 徽标 + 卖点一「黄蓝装基底手动分档，识别更精准」定稿；描述句、卖点二三四占位待定
- 程序化验证全绿：笔刷分档/改档/取消/已分级联动/移出、全量列内降序、白装高亮→追加→孔数过滤→重置、暗金分档数学/组合重算/恢复原序；桌面 1280 与移动 390 三 tab 无溢出

**✅ 已完成：04 功能长廊 · 第五幕「主题编辑器」（2026-09-15 用户验收完结；标题/描述/卖点二占位待定稿）**

`features/ThemeAct.vue`，布局同第四幕（文案横排在上 + 演示面板全宽），POE1/POE2 双版本通用不挂徽标。面板 = **左右两分区 + 底部操作行**：

- 左分区：配色组多选圈选（5 组真实风格配色：红字白底 34 块/黄字暗底 23/橙字白底 21/黑字橙底 19/灰字暗底 18），默认全选，实时显示「已圈选 N 组 · N 个过滤块」
- 右分区：**签名叙事区式满屏散落**——23 个名牌 left/top/rot 全部手排（各组物品交错混排、±2° 微旋转、纵向 5%~81%），进场错峰下落浮现；物品名来自签名区/Hero 真实掉落池
- 底部操作行：当前主题名 + 「应用到过滤」（**循环切换三套预置主题**：鎏金纪元/深海静谧/翡翠之梦，每套对 5 组各有目标配色）+「还原配色」；圈选组名牌错峰 pop+辉光+变色（0.3s 过渡），未圈选组恒为原色，取消勾选即时回原色
- 交互定案（用户口径）：左侧勾选**只决定哪些组被换**，与右侧物品显隐无关；右侧无任何改色控件，纯演示"一键换主题"
- 幕底一行四张截图（主题编辑器1~4.webp：批量改色/配色组圈选/当前预览/主题市场）；主题市场按用户定只进卖点文案
- 验证：三套主题循环/取消组回原/还原/桌面移动零越界零溢出全绿
- 迭代轨迹：①初版三区联动（配色组+名牌墙+六参数外观编辑器 draft 实时变色，名牌整齐排列）②用户反馈要"签名区式散落+一键全换更有沉浸感"→ 改多组圈选+满屏散落+应用按钮 ③用户再定方向：去掉改色控件、只留两分区+底部按钮+预置主题循环 → 终版
- 教训：flex 容器裸文本 ellipsis 不生效（必须包 span）；手排散落最右列要按名牌宽度留右缘余量（390px 下长名截断 max-width 26vw）；**存量问题**：768~1279 宽度段 VitePress 导航栏 VPNavBarExtra 自身溢出 ~136px（主题层，与首页无关，待用户定是否修）

**➡️ 下一步：04 功能长廊 · 第六幕「云过滤同步」（用户定：最后一幕）**——开工前对齐形态与素材；第六幕定档后各幕眉题「/ 05」需统一改为「/ 06」。后续队列：05 Bento → 06 社区生态带 → 07 CTA 终章 + 全页联动检查。遗留待办：第五幕描述句/卖点定稿。

**备选想法（讨论过未做）**：把旧首页的手绘涂鸦下划线 SVG path 搬进 Hero，用 DrawSVG（gsap 3.13+ 免费）做描边动画替换现在的直线下划线——用户已知悉，想做随时可加。

**后续队列**：05 Bento → 06 社区生态带 → 07 CTA 终章 + 全页联动检查。

**每块验收标准（两态）**：桌面全动画 / 移动端排版正确+动画轻量。（2026-09-15 起不读 `prefers-reduced-motion`，见设计定案；原"减少动效直接终态"态作废）

## 四、注意事项（踩过的坑）

1. **dev server**：用户自己起在 **5173** 端口，AI 检查一律用 `http://localhost:5173/`，**不要**另起 5273
2. VitePress 是 SSG：GSAP/DOM 代码全部放 `onMounted`；公告弹窗若要内容更新在 `NoticeContent.vue`
3. 右下角 Teek 悬浮件隐藏有坑：**回顶按钮（`.back-top`）和壁纸按钮在同一个容器 `.tk-right-bottom-button` 里**，只能藏 `.tk-right-bottom-button__button:not(.back-top)`，藏容器会连坐回顶；公告铃铛 `.tk-notice__icon` 单独藏。另：Teek 回顶只响应真实滚轮/平滑滚动，编程式瞬移滚动不算数（全站行为，验收时用滚轮）
4. **GSAP 坑（重要）**：嵌入式浏览器/后台标签页会节流 rAF，GSAP 默认 `lagSmoothing` 会让入场时间线"爬行"（数秒只走 2%），必须在 onMounted 里 `gsap.ticker.lagSmoothing(false)`，后续所有区块照做
5. **GSAP 坑 2**：待机循环动画（漂浮/呼吸）和入场动画会争夺同一元素的 opacity/transform——待机必须等入场结束再启动（`tl.eventCallback("onComplete", ...)`）
6. **ScrollTrigger 坑**：若 trigger 元素高度由 mode class 切换驱动（如签名区 320vh），必须在 `nextTick` DOM 更新后再 `ScrollTrigger.create` 并 `ScrollTrigger.refresh()`，否则进度区间按旧尺寸计算（症状：刚滚一点 progress 就冲满）；钉住优先用 CSS `position: sticky`，ST 只算进度，避免 pin-spacer 在 SSG 的各种坑
7. 构建噪音清单（均与本改版无关，忽略）：`localStorage is not defined` ×37（Teek）、`document is not defined` ×1（06.POE网址导航.md 页面脚本存量问题）
8. 版本号徽章（v3.29.2 / v2.0.8）目前写死在 HeroSection 模板里，未来可改为读 siteConfig 接口
9. 亮色偏好用户直开首页理论上有"导航先亮后暗"一瞬，观感明显再加内联脚本纠正
10. `docs/` 下的 .md 都会被构建成线上页面，开发文档放 `docs/.vitepress/` 下（本文件因此在这里）
11. 首页旧内容（QQ群/广告/用户数）已从 index.md 移除，将在 04/05/06 区块中以新形态回归
12. 素材来源：物品图标/命运卡图/掉落地面图从 `yl-editor` 的 public 与组件资产里拿（POE1/PO2 双套图标在 `yl-editor/src/data/category.ts` 体系）
13. **协作流程（用户定）**：视觉验收一律由用户自己做，AI 只做 DOM/数值程序化检查，不要派视觉审查子智能体（慢）；文档只记用户验收过的状态，每块做完先给用户看，OK 后再写入本文档；**commit/push 由用户自己执行，AI 不主动提交，只提供提交信息（2026-09-15 补充）**
14. 连续编辑文件时 Vite HMR 会整页热重载产生竞态假象（动画看似卡死），排查前先干净 reload 一次再下结论
15. **构建管线会吃掉 transition 简写里的 `var()`**（2026-09-15 实锤）：`transition: opacity .65s var(--yl-ease-out)` 编译后浏览器收到的是空 longhand（`transition-duration: ;`）——全站 CSS 过渡曾因此时长全为 0、全部瞬变。**规则：transition/animation 简写里不写 var()**，要么缓动用字面量，要么拆成 longhand（单值 longhand 如 `transition-timing-function: var(--x)` 可存活）。排查手法：读 `getComputedStyle().transitionDuration` 和浏览器侧规则 cssText，别只看源码逻辑
16. **主题自带 a11y 全局减少动效重置**（2026-09-15）：`@media (prefers-reduced-motion: reduce) { *, ::before, ::after { transition-duration: 0s !important; animation-duration: 1ms !important; animation-iteration-count: 1 !important; … } }`——用户机器开着 Win11「减少动效」时全站 CSS 过渡/动画被压扁（GSAP 动画不受影响，所以签名区/Hero 一直正常、从未暴露）。按"首页动画无条件执行"定案，HomePage 挂载时运行时把该媒体规则 `mediaText` 改为 `not all` 停用、卸载时原样恢复（只停命中 `*` 全局重置的那条，不动组件级降级）
17. **一次性进场动画两坑**（2026-09-15）：①载入时元素已在视口（滚动恢复/锚点直达）时 `is-in` 若在首帧绘制前挂上，过渡直接跳终态——触发要包**双 rAF**（先让隐藏初始态绘制一帧）；②**时间兜底不能无条件触发**（曾因 3 秒兜底在屏幕外把后续区块动画全提前播完），只应在"元素已进视口但没收到 IO 回调"的异常时补触发

## 五、改动记录（按块追加）

### 2026-09-14 · 地基（第1块）

- 新建 `home/` 组件目录：tokens.scss + HomePage.vue + 6 个 section 空壳
- `index.md` → `layout: page` + `<HomePage />`；theme/index.ts 注册组件
- 安装 `gsap@3.15.0`
- 全宽覆盖、强制深色、隐藏首页明暗切换（`yl-home-active` 机制）、顶部金色氛围光晕（已实测可见）
- 用户改动：teekConfig 公告 `initOpen: false`

### 2026-09-14 · 02 Hero（第2块，已验收）

- HeroSection 全量实现（入场时间线/SplitText/logo/双CTA/数据带/SCROLL/锚定光晕/金尘/7个过滤掉落物名牌+三层光柱+掉落演出），细节见上方"当前状态"
- HomePage：隐藏 Teek 悬浮件（公告铃铛/壁纸按钮，回顶保留）、基础顶部光晕降至 0.1
- 修复：GSAP lagSmoothing 爬行 bug（注意事项4）；待机与入场争夺 opacity（注意事项5）
- 用户反馈迭代（共 5 轮）：删眉题、教程改按钮、用户数 SSR 初始 0 消闪烁、副标题两行、版本徽章独立行且改为圆点+文本非按钮样式、回顶按钮恢复、SCROLL 加强、数据带字号加大、加 logo、大光圈改锚定光晕+金尘、代码碎片改真实过滤名牌、+3 个名牌（贪婪战书/重革腰带/毁灭者之弓）、名牌散落感（dx/mt/bh）
- 验收：桌面+移动程序化检查全绿（用户目视定稿）

### 2026-09-14 · 03 签名叙事区（第3块，已验收）

- SignatureSection 全量实现：真实过滤样本场景（21 物品/真实配色/13 图标/5 光柱）+ 桌面滚动叙事（sticky 钉住 320vh、扫描线扫过、垃圾坠落、原地点亮、三段文案、可逆）+ 移动端/减少动效滑杆降级（双层反向 clip）
- 迭代记录：从左往右揭示方向、无过滤态真实默认样式（150 141 121 / 13 12 10）、舞台加高至 660、光柱 3×52、双层反向裁切修复"垃圾未隐藏"bug、拉伸范围放开 1~99、纵向紧凑 14~78%、真实样本替换、滚动模式 ScrollTrigger nextTick 修复
- 文案：已由用户 2026-09-15 定稿（见当日改动记录）

### 2026-09-15 · 动画策略变更：无条件执行，不读系统 prefers-reduced-motion（已验收）

- 起因：用户公司电脑 Win11 关闭「辅助功能-动画效果」→ 浏览器上报 `prefers-reduced-motion: reduce` → 首页动画全部消失；用户要求动画无论系统设置一律执行，且**后续所有设置都遵循此需求**
- HeroSection：`mm.add` 条件从 `(prefers-reduced-motion: no-preference)` 改为恒真 `(min-width: 0px)`（保留 matchMedia 只为沿用统一 revert）；删掉 reduced 分支与两段 `@media (prefers-reduced-motion: reduce)` 禁用光柱呼吸/SCROLL 呼吸线的规则
- SignatureSection：scroll 模式门槛 `(min-width: 960px) and (prefers-reduced-motion: no-preference)` → `(min-width: 960px)`
- 程序化验证（本机真实上报 reduce 的环境下）：SplitText 拆字、7 名牌 GSAP 内联样式、SCROLL 呼吸 CSS 动画激活、签名区 `yl-sig--scroll` 320vh 全部正常
- **此后 04~07 所有区块一律遵守：不新增任何 prefers-reduced-motion 判定；验收标准由三态改为两态**
- 同日：用户数跳动提速（待用户目视）——`fetchCount` 改为挂载即并行请求；`.call` 从时间线末尾（≈3.5s）提前到数字行开始淡入处（`"<"` 位置，≈2.2s），数据就绪即跳，不再等掉落物演出播完
- 同日：协作规则变更——AI 不再主动 commit/push，只提供提交信息，由用户自己提交
- 同日：签名叙事区文案定稿（用户提供）——标题「同样的掉落，不同的效果」、滑杆描述前半句「看看过滤器是什么效果」、phases 三段全部替换，桌面滚动模式三段实测渲染正确；其后标题再改为「一眼看懂过滤是什么」，滑杆描述尾巴与滚动描述定稿为「垃圾消失，各分类价值清晰可见」「继续滚动，让过滤器持续加载」

### 2026-09-15 · 04 功能长廊第一幕「可视化编辑」（暂定稿）

- 初版为滚动叙事（340vh sticky + ScrollTrigger 五阶段：灰牌→红字→白底红框→字号缩→光柱升起 + 可交互彩蛋），经用户多轮反馈最终改为**纯交互演示**，组件内 GSAP/ScrollTrigger/sticky 全部移除
- 迭代轨迹（共 10 轮）：①区块大标题「一乐过滤能做什么？」②新增边框色板 + 小地图图标开关（6 行控件）③取消左右列滚动联动、改入场从下往上浮现 ④光柱先换 Hero 三层放大版 ⑤文案多轮定稿 ⑥面板选中态脱离滚动 ⑦预览也去滚动联动 + 区块/地面高度缩小 ⑧默认终态（红字白底红框+光柱图标全开）、字号钳制 12~45、名牌+光柱整体居中 ⑨光柱由 CSS 柔光柱改回编辑器原素材 cross-Red.svg（放名牌右侧）⑩删底部提示小字
- 图片资源策略（用户定）：全部在线直链 edit.filtereditor.cn，不复制进仓库
- 排查记录：内嵌浏览器窗格被遮挡时渲染器整体挂起（rAF 0 帧、scroll 事件不派发、程序化滚动假性失效，连已验收的签名区都像坏了）——此时应改用直接注入组件状态的方式验证逻辑；窗格可见/真实滚轮时一切正常（与注意事项 3/14 同源，本轮已两次踩中，后续程序化检查前先探 rAF）

### 2026-09-15 · 04 功能长廊第二幕「物价排序」（已验收完结）

- **结构重构先行**：FeaturesSection 拆为容器 + `features/` 目录每幕独立组件（VisualEditAct / PriceSortAct / ShotThumb），第一幕内容零改动迁入，后续幕照此加文件
- 初版：FLIP 行排序（TransitionGroup move）+ 行内 T0~T3 徽标 + T0 游戏样式名牌 + 自动首播
- 迭代轨迹：①修 TransitionGroup 整组换 key 时旧行占位一帧导致"向下闪烁"（leave-active 立即 absolute 脱流）②用户描述编辑器真实布局后**重做为"左参考 / 右分级"形态**（左列表原序固定，右侧点排序生成分级组），FLIP/徽标/T0 名牌随之废弃 ③删毒浸信仰、暗金删阿克莱草原——六类统一 6 物品，切 tab 高度零跳动 ④T0 名牌按用户要求移除 ⑤贴编辑器实际截图并抽 ShotThumb 公共组件 ⑥文案定稿：标题「物价排序，实时物价分级」+ 标题右上角 POE1 徽标（POE1 编辑页专属功能标记，后续 POE1 专属的幕同款）、描述句「——物价变了，分类T级跟着一键同步」（卖点四条仍占位待用户定稿）
- 素材：物品名取自 json-data/poe1 真实数据；价格示例值；截图用户提供（public/assets/index/）
- 排查记录补充：TransitionGroup 整组替换必须处理 leave 占位（absolute + 立即隐藏），否则下方内容闪跳；lightbox 程序化测关闭时要考虑离开过渡在 rAF 冻结时不结束，读 DOM 会误判"没关上"

### 2026-09-15 · 04 功能长廊第三幕「通货、宝石规划器」（已验收完结）

- 幕结构变更（用户定）：POE2 规划器拆两幕——通货 + 宝石合一幕（形态同物价排序），装备规划器单开第四幕（待做）
- PlannerAct 实现：双层 tab（规划器切换 + 通货子分类）+ 左参考/右分级 + 排序/自动首播 + POE2 徽标
- 数据挖掘：`json-data/poe2/通货.json`（613 条扁平表）里挖出规划器五分类真实物品名（次级沙漠符文 / 塔卡提的灵核 / 巨熊雕像…）+ 血脉辅助宝石真名（布鲁特斯之脑…）；价格示例 E 值待校正
- 迭代轨迹：①标题「通货、宝石实时规划」②卖点三「按实时物价自动分档，配色自由修改」③描述句「——档位跟着当前物价走」④灯箱放大图 1920px/90vh ⑤徽标定位三轮定案：绝对贴块右缘（离字远）→ 行内（长标题被挤断行）→ **fit-content + 绝对右外侧 -58px + 移动端行内回退**（两幕统一）
- ShotThumb 增强：图片缺失整块降级不渲染（截图未放时预留位不破版）

### 2026-09-15 · 长廊全段进场动画 + 全站 CSS 过渡失效修复（已验收）

- 长廊从大标题「一乐过滤能做什么？」起全部元素加进场浮现：上浮 24px + 淡入 0.65s、按 眉题→标题→描述→卖点→面板→行→预览/截图 错峰（对齐 Hero 副标题手感）；四个组件（容器+三幕）各自 IO + 双 rAF 触发 + 视口内守卫兜底
- 修复三层"过渡从未生效"（详见注意事项 15~17）：主题 reduce 全局重置（HomePage 运行时停用）／transition 简写 var() 被编译清空（全站 9 处拆 longhand，含签名区名牌点亮、Hero 按钮悬停等存量代码）／载入已在视口时双 rAF
- 附带：Hero 底部「SCROLL」改「向下滚动」
- 教训：动画"没变化"类反馈，先查 `getComputedStyle` 的过渡时长/规则 cssText 再查逻辑——本轮真凶全是 CSS 层，逻辑层毫无问题

### 2026-09-15 · 04 功能长廊第四幕「装备规划器」（已验收完结）

- 另记（早前同日）：Hero 按钮行在「打开编辑器」后新增「物价榜介绍」按钮（微信公众号文章外链，新标签打开，桌面/移动验证无溢出）
- 形态讨论定案：通读 yl-editor `equipment-advance-mode` 三 tab 真实实现后定 **mini 复刻**路线而非再造演示形态；布局破例**全宽面板**（文案上/演示下，用户要求区域做大）；三 tab 同编辑器结构（黄蓝装/白装/暗金），先做黄蓝装、再白装、再暗金逐 tab 验收
- 黄蓝装：三视图（已分级三列 / 武器卡片平铺 / 防具分列）+ 笔刷交互 + 全量真实数据（弓 25 + 胸甲 136，脚本生成 `equip-data.ts`，数据与组件分离）
- 迭代轨迹：①防具 7 列全上→用户嫌换 2 行太长→收敛 4 列展示（数据留全量，`SHOWN_GROUPS` 挑组）②预置分级按用户口径重排（T1=3 弓+3 纯组榜首 / T2、T3=各展示组降序第 2/3 名）③标题三轮：装备规划，分级你说了算 → 黄蓝装规划，T级精准操控 → 装备规划，T级精准操控（覆盖三 tab）④白装块结构：用户先想换 81/82 后撤回，保持 82白/3孔T1/3孔T2 ⑤暗金：加猎首（重革腰带 300E，经典）、删意志呼唤（黄玉戒指图标线上 404）
- 白装/暗金交互对齐编辑器行为：多孔块按孔数过滤填充池、同一基底可入多块、T0 单一基底只读（判定来自传奇装备.json 统计）；演示简化处：门槛用预设按钮/组合（编辑器为自由输入）、暗金已排序切组合实时重算（编辑器需重点排序）
- 教训：expand json 里的 icon 路径线上可能 404（a6a2f91559343 黄玉戒指实测），新图标上线前先 curl 验证；程序化测试连续操作 Vue DOM 时 evaluate 需逐步 await（同步读的是旧 DOM），且内嵌窗格勿用 rAF 等待（会挂起，用 setTimeout）
- 待办：描述句/卖点二三四定稿、总幕数定稿后眉题「/ 05」统一改
- 补记（同日晚）：幕底接入用户提供的五张编辑器实际截图（一行五列、按 tab 顺序），ShotThumb lightbox 复用；至此第四幕截图位闭环

### 2026-09-15 · 04 功能长廊第五幕「主题编辑器」（已验收完结）

- 通读 yl-editor `theme-editor`（四 tab：批量改色/当前预览/主题市场/我的主题）+ 用户提供的四张截图后定形态：聚焦「一键换主题」交互演示，主题市场只进卖点文案
- ThemeAct 实现：文案上 + 全宽面板两分区（配色组多选 / 签名区式手排散落名牌墙）+ 底部操作行（三套预置主题循环：鎏金纪元/深海静谧/翡翠之梦）
- 迭代轨迹（两轮用户反馈）：①初版三区联动（含六参数外观编辑器、名牌整齐排列）②"要签名区那样的散落、一键全部换、更有沉浸感"→ 散落+多选圈选 ③"右侧不要改色控件，左右两分区+按钮在下，预置主题即可，勾选只为演示一键换主题"→ 终版定型
- 排查记录：内嵌窗格后台时 CSS transition/animation 冻结会造成功效采样假象（读数停在过渡起点，如 font-size 15px 实为 15→16 中间态）——判断依据：读 computed 与内联/状态一致性，别急着改逻辑
- 教训：flex 容器裸文本 text-overflow 不生效必须包 span；散落手排要按最长名牌留右缘余量；768~1279 VitePress 导航栏 VPNavBarExtra 溢出 ~136px 为主题存量问题（待用户定）
- 待办：描述句/卖点定稿；第六幕（云过滤同步）定档后各幕眉题「/ 05」→「/ 06」统一改
